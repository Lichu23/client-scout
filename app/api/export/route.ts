import { db } from "@/lib/db";
export async function GET() { const leads = await db.lead.findMany({ orderBy: { createdAt: "desc" } }); const fields = ["businessName","businessType","location","instagram","website","opportunity","leadScore","status"]; const esc = (v: unknown) => `"${String(v ?? "").replace(/"/g, '""')}"`; const rows = [fields, ...leads.map(l => [l.businessName,l.businessType,l.location,l.instagramUrl,l.websiteUrl,l.opportunity,l.leadScore,l.status])].map(r => r.map(esc).join(",")); return new Response(rows.join("\n"), { headers: { "Content-Type": "text/csv; charset=utf-8", "Content-Disposition": "attachment; filename=leads.csv" } }); }
