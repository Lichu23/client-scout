import { db } from "@/lib/db";
export default async function Dashboard(){const [total,newCount,contacted,replied,clients]=await Promise.all([db.lead.count(),db.lead.count({where:{status:'NEW'}}),db.lead.count({where:{status:'CONTACTED'}}),db.lead.count({where:{status:'REPLIED'}}),db.lead.count({where:{status:'CLIENT'}})]);return <div><h1 className="text-3xl font-bold">Dashboard</h1><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{[['Total leads',total],['New',newCount],['Contacted',contacted],['Replies',replied],['Clients',clients]].map(([l,n])=><div className="rounded-xl border bg-white p-5" key={String(l)}><p className="text-sm text-slate-500">{l}</p><p className="mt-2 text-3xl font-bold">{n}</p></div>)}</div><a href="/api/export" className="mt-8 inline-block rounded bg-blue-600 px-4 py-2 text-white">Export CSV</a></div>}
